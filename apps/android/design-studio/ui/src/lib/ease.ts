/** Shared easing curves for motion/CSS transitions. */

/** Smooth ease-out — quick start, gentle settle. Good for fades and reveals. */
export const EASE_OUT = [0.16, 1, 0.3, 1] as const

/** Symmetric ease-in-out for interruptible state changes. */
export const EASE_IN_OUT = [0.65, 0, 0.35, 1] as const
